{
  "name": "reboot.one",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "esbuild --bundle index.js --format=esm --outfile=docs/index.b.js --servedir=docs/",
    "build": "esbuild --bundle index.js --format=esm --outfile=docs/index.b.js"
  },
  "devDependencies": {
    "esbuild-cli": "^0.0.1"
  },
  "dependencies": {
    "@noble/curves": "^1.0.0",
    "@socketsupply/tonic": "^15.1.1",
    "nostr-tools": "^1.13.1",
    "powmem": "^1.1.2"
  }
}
